'use strict';

var expect = require('expect');
var config = require('../../../lib/commands/config');

describe('command: config', function() {

  it('should print all the configurations', function(done) {
    done(0)
  });

});
